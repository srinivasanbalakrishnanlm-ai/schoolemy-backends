{
  "name": "schoolemy-node-user-backend",
  "version": "1.0.0",
  "description": "Backend API for Schoolemy User System — built with Express, MongoDB, and ES6 modules.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules DISABLE_HTTP_LISTENER=true jest --runInBand",
    "test:watch": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules DISABLE_HTTP_LISTENER=true jest --watch",
    "test:unit": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules DISABLE_HTTP_LISTENER=true jest --runInBand --selectProjects unit",
    "test:integration": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules DISABLE_HTTP_LISTENER=true jest --runInBand --selectProjects integration",
    "test:coverage": "cross-env NODE_ENV=test NODE_OPTIONS=--experimental-vm-modules DISABLE_HTTP_LISTENER=true jest --runInBand --coverage"
  },
  "keywords": [
    "nodejs",
    "express",
    "mongodb",
    "backend",
    "api",
    "schoolemy",
    "user system"
  ],
  "author": "Schoolemy Team",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.3",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.10",
    "razorpay": "^2.9.6",
    "serverless-http": "^4.0.0",
    "sharp": "^0.34.5",
    "twilio": "^5.10.5"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.5",
    "babel-jest": "^30.2.0",
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^10.3.0",
    "nodemon": "^3.1.11",
    "supertest": "^7.1.4"
  }
}
